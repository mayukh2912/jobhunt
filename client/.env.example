# Set this to your deployed backend URL when hosting the frontend
# (e.g. on Vercel/Netlify). Leave blank for local dev — Vite's proxy
# handles /api requests to localhost:5000 automatically.
VITE_API_URL=
